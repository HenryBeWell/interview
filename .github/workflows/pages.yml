name: Build and Publish My GitBook

on:
  workflow_dispatch:
  push:
    branches:
      - gh-pages

jobs:
  build-and-deploy:
    name: Build and deploy
    runs-on: ubuntu-latest
    env:
      MY_SECRET   : ${{secrets.DEPLOY_KEY}}
      USER_NAME   : henrybewell
      USER_EMAIL  : zhanghengruic@gmail.com
      BOOK_DIR    : interview

    steps:
    - name: Checkout 🛎️
      uses: actions/checkout@v2.3.1

    - name: Build and Deploy 🚀
      uses: onejar99/gitbook-build-publish-action@v1.0.2